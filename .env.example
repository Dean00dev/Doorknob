# Safe default: no external model call and no API cost.
MODEL_PROVIDER=mock

# To opt into Anthropic, set MODEL_PROVIDER=anthropic and provide both values.
# Never put a real key in a file committed to source control.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=
PORT=8787
